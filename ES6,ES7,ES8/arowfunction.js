const soma = (a, b) => {
console.log(a + b);
};

//e apenas uma forma diferente de usar a função porem tem uma funcionalidade diferente em orientação a objeto
soma(10, 40)

//quando sua linha tiver apenas um parametro n precisa usar {} e fica bem encurtada
var mult = a => console.log(a*2);

//como funciona o return numa arrowfunction, apenas quando tem so uma linha
var mult1 = (a, b, c) =>  a * b * c;

